/// Annotation that marks a class for automatic `copyWith` method generation.
///
/// Apply this annotation to a class to generate a type-safe `copyWith`
/// extension method via the `easy_copy_with` code generator.
class CopyWith {
  const CopyWith();
}
